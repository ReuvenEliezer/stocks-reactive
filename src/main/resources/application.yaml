server:
  port: 8080
spring:
  application:
    name: @project.artifactId@
#  codec:
#    max-in-memory-size: 3999962144MB
  profiles:
    active: dev
  rsocket:
    server:
      port: 8081
  output:
    ansi:
      enabled: always


scheduler:
  thread:
    pool:
      size: 5
